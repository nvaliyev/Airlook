import {StyleSheet} from 'react-native'
const styles = StyleSheet.create({

    Splashlogo:{
        width: '80%',
        resizeMode:'contain',
    },
logo : {width: 200, 
    resizeMode:'contain', 
    maxHeight:250}
})

export default styles